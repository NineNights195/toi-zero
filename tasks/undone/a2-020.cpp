#include<bits/stdc++.h>
#include<iostream>
using namespace std;

int findCircleSize(int n){
    int current=n;
    int count=0;

    while()
}

int main(){
    int n;
    cin >> n;
    int passTo[n];
    for(int i=0 ; i<n ; i++){
        cin >> passTo[i];
        --passTo[i];
    }
    bool visited[n] = {false};
    int answer=0;
    while()
}